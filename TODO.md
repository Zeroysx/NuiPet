# TODO

## v0.2.2 Review Follow-ups

- Evaluate whether the drag-only row names should change from `happy_run` and `run` to direction-oriented names such as `run_right` and `run_left`. Keep compatibility aliases if this is changed after v0.2.2.
- Revisit the `sleep` compatibility alias currently mapped to `cry`. v0.2.3 should prefer a real `sleep` action when the animation asset is ready.
- Redesign or merge single-click and double-click reaction groups so the two interactions have visibly distinct behavior.
- Decide whether the long thinking / long idle action remains useful after the v0.2.3 animation pass.
