# Repository Instructions

- Do not work directly on the `main` branch. Create a new branch for all changes and merge into `main` only after review.
- Every completed edit must update both `CHANGELOG.md` and `README.md`.
- `CHANGELOG.md` is append-only.
