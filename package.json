{
  "name": "nuipet",
  "version": "0.1.0",
  "private": true,
  "description": "A Tauri desktop pet based on 鹿弈Nui.",
  "scripts": {
    "dev:web": "node scripts/dev-server.js",
    "build:web": "node scripts/check-assets.js",
    "tauri:dev": "cargo tauri dev",
    "tauri:build": "cargo tauri build",
    "check": "npm run build:web && cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {}
}
